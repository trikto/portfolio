# Public surface: host api.gajan.dev, path Prefix /api/v1/files only.
# /healthz, /readyz, and /metrics are intentionally NOT on this Ingress.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: file-share
  namespace: files
  labels:
    app.kubernetes.io/name: file-share
    app.kubernetes.io/part-of: file-share
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.gajan.dev
      secretName: gajan-dev-tls
  rules:
    - host: api.gajan.dev
      http:
        paths:
          - path: /api/v1/files
            pathType: Prefix
            backend:
              service:
                name: file-share
                port:
                  name: http
