apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Certificate lives in namespace secrets; include the Namespace so this
  # package can be applied before deploy/onetime-secret if needed.
  - ../onetime-secret/namespace.yaml
  - clusterissuer.yaml
  - certificate.yaml
