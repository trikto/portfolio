import Link from "next/link";
import { services } from "@/data/services";

export function ServicesPreview() {
  return <section id="services" className="section services-preview" aria-labelledby="services-preview-title"><div className="section-heading"><div><p className="eyebrow">SERVICES</p><h2 id="services-preview-title">DevOps support for reliable application delivery</h2></div><p className="section-note">Available for selected freelance and contract DevOps projects.</p></div><p className="services-intro">I help small teams and growing products automate deployments, operate cloud infrastructure, improve observability, and resolve production issues.</p><div className="services-grid">{services.map((service) => <article className="service-card" key={service.title}><span className="service-icon" aria-hidden="true">{service.icon}</span><h3>{service.title}</h3><p>{service.shortDescription}</p><ul>{service.deliverables.slice(0, 3).map((item) => <li key={item}>{item}</li>)}</ul><Link href="/services">View service <b aria-hidden="true">→</b></Link></article>)}</div><div className="services-actions"><Link className="button primary" href="/services">View all services <span aria-hidden="true">→</span></Link><a className="button ghost" href="mailto:gajanrajah@protonmail.com?subject=DevOps%20project%20discussion">Discuss a project <span aria-hidden="true">↗</span></a></div></section>;
}
